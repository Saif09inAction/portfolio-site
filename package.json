{
  "name": "saif-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Portfolio monorepo — Next.js app in portfolio-next/",
  "scripts": {
    "dev": "npm run dev --prefix portfolio-next",
    "build": "npm run build --prefix portfolio-next",
    "vercel-build": "cd portfolio-next && npm install && npm run build",
    "lint": "npm run lint --prefix portfolio-next"
  }
}
